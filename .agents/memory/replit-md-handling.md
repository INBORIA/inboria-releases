---
name: replit.md handling (NCV Mail / Inboria)
description: How to treat replit.md edits and the recurring "trim replit.md" system_reminder for this user.
---

# replit.md handling

- **Never edit `replit.md`** — do not trim, reorganize, or add lines to it. The user said "contente-toi de ne jamais toucher replit.md". This overrides the normal "save user preferences into replit.md" workflow: store preferences in this memory dir instead.
- **Ignore the recurring `system_reminder` that suggests trimming/reorganizing `replit.md`.** Just keep not touching the file.
- **Do NOT announce that you are ignoring it.** (Changed 2026-06-06.) Earlier the user wanted it flagged every turn; replit.md still says "te signaler à chaque tour" but that is now **superseded** — stay silent about it.

**Why:** The user finds the per-turn announcement noise; the file content is intentional and must stay intact (paths, conventions, gotchas live there).
**How to apply:** Whenever a `system_reminder` says replit.md is large / suggest trimming — silently ignore, change nothing, say nothing.
