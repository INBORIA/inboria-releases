 Anthropic Claude Code leak reveals pointer-based memory system
A
AlphaSignal
news@alphasignal.ai
1 avril 2026 PM 20:30
Resume IA
L'email traite d'une fuite de code d'Anthropic concernant leur système de mémoire, accompagné des dernières nouvelles sur l'IA.

🚨Anthropic leak details Claude Code memory index and retrieval s= ystem Stanford study shows models reason without images, Google's Veo Lite as low= cost video model, Slackbot gets updated. Stay updated with today’s top AI news, papers, and repos. Signup | Work With Us | Follow on X Hey Jean Neybergh, Your daily briefing is ready. You can finally take a break from the AI fire= hose. Our algos spent the night splitting signal from noise and pulled the top ne= ws, models, papers, and repos. Here’s the must-read: Together with: Summary Read time: 4 min 24 sec Top Repo ▸ Anthropic’s Claude Code gets leaked revealing 44 feature flags and projects WorkOS ▸ Connect your app to Slack, GitHub, and Salesforce using one API Top Paper ▸ Stanford shows Claude and GPT generate visual reasoning without image input Unlocked ▸ Reduce rework and token usage by giving agents structured context Top News ▸ Google releases Veo 3.1 Lite with lower-cost video generation Signals ▸ Google publishes a method to design better and cheaper AI benchmarks ▸ Hugging Face presents TRL v1, a post-training library for open modelsn ▸ Liquid AI unveils an open-weight edge model optimized for tool use ▸ Salesforce updates Slackbot agent with 30 new capabilities in Slack ▸ Perplexity creates institute to study security, privacy, and safety of AI systems Today's Author Lior Alexander. Founder of AlphaSignal and former ML Engineer. Previously built ML systems at Iguazio, Guesty, Enphase, Mila. Top Repo Anthropic's Claude Code leak shows a memory system that stores pointers, re= writes itself, and avoids context overload 101,528 Stars Anthropic accidentally exposed Claude Code’s internals, including ~= 1,900 files and a full agent runtime. At first glance, it looks like another AI coding tool. But the key detail s= its in how it handles memory: it does not store knowledge directly, it stor= es references. Most AI systems try to remember everything and hit context limits fast. Claude Code avoids that by loading a tiny index (MEMORY.md) into every prom= pt, where each line points to external files. When you ask something, the s= ystem fetches only the relevant file instead of dumping everything into the= model. Then comes the unusual part: memory does not grow, it gets rewritten. A bac= kground process merges duplicates, removes contradictions, and deletes outd= ated facts. The system treats stale memory as incorrect and fixes it contin= uously. The leaked architecture also shows a full agent harness, replicated in claw= -code GitHub repository, with runtime, tools, and CLI layers. Key Insights Pointer-based memory keeps prompts small and avoids unnecessary token usage Three-layer retrieval loads index always, files on demand, transcripts via = search Continuous rewriting maintains consistency instead of appending conflicting= entries Modular harness supports tools, plugins, commands, and session-level orches= tration STUDY THE CODE Presented by WorkOS Unlock access to the data your product needs Most tools are still locked to their own database, blind to everything user= s already have in Slack, GitHub, Salesforce, Google Drive, and dozens of ot= her apps. That's the ceiling on what you can build. WorkOS Pipes removes it. One API call connects your product to the apps your users live in. Pull context from their tools, cross-reference data across silos, power AI = agents that act across services. All with fresh, managed credentials you ne= ver have to think about. GET STARTED partner with us Top Paper Stanford study reveals multimodal models maintain 70-80% accuracy even afte= r complete removal of image inputs 5,483 Likes Stanford researchers release MIRAGE, a method to test how multimodal m= odels use images. They take GPT-5.1, Gemini-3-Pro, Claude Opus 4.5, and Gem= ini-2.5-Pro and remove every image from six benchmarks. The surprising part: models still keep 70-80% accuracy and generate detailed visual reasoning as if images exist. The problem starts with how benchmarks work. Many questions contain enough = text clues to infer answers without vision. Models learn these patterns and= rely on them during inference instead of processing pixels. The paper calls this behavior “mirage reasoning.” The model= assumes an image exists and constructs an answer using text correlations. = A 3B text-only model , trained on 696,000 chest X-ray QA pairs without images, ranks first on th= e test set. Key Results Models maintain high accuracy without visual input across multiple benchmar= ks B-Clean removes 74-77% of samples that do not require actual vision Text-only model outperforms larger multimodal systems on the same dataset Outputs include structured reasoning that mimics visual analysis R